{
  "name": "userhub-render-fallback",
  "private": true,
  "scripts": {
    "start": "cd ../frontend && npm run preview --host 0.0.0.0 --port ${PORT:-3000}",
    "build": "cd ../frontend && npm install && npm run build"
  }
}
